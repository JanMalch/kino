import { SeatMatrixPipe } from './seat-matrix.pipe';

describe('SeatMatrixPipe', () => {
  it('create an instance', () => {
    const pipe = new SeatMatrixPipe();
    expect(pipe).toBeTruthy();
  });
});
