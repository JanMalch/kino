import {FilterPastPipe} from './filter-past.pipe';

describe('FilterPastPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPastPipe();
    expect(pipe).toBeTruthy();
  });
});
