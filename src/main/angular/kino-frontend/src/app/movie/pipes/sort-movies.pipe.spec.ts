import {SortMoviesPipe} from './sort-movies.pipe';

describe('SortMoviesPipe', () => {
  it('create an instance', () => {
    const pipe = new SortMoviesPipe();
    expect(pipe).toBeTruthy();
  });
});
